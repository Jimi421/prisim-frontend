/** @type {import('open-next').OpenNextConfig} */
module.exports = {
  // Specify the deployment adapter
  wrapper: "cloudflare-pages",
  // Optional: add output directory if you want to customize (usually not needed)
  // outDir: "./.open-next", // default is .open-next
  // Optional: custom function dir if you want (usually not needed)
  // functionsDir: "./functions",
};

